<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;600;700&family=Sarabun:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/ptData.css">
    
</head>
<body>
    <header class="header">
        <div class="header-content">
            <h1>
                <i class="fas fa-chart-line"></i>
                <%= title %>
            </h1>
            <div class="user-info">
                <span><%= user.fullname || user.username %></span>
            </div>
        </div>
    </header>

    <nav>
        <a href="/index"><i class="fas fa-home"></i> หน้าหลัก</a>
        <a href="/examinationroom/<%= patient ? patient.HN : '' %>"><i class="fas fa-stethoscope"></i> ห้องตรวจ</a>
        <% if (patient) { %>
            <a href="/ptdata/<%= patient.HN %>" class="active"><i class="fas fa-chart-line"></i> สรุปผลการรักษา</a>
        <% } else { %>
        <% } %>
    </nav>

    <div class="main-container">
        <!-- Alert Messages -->
        <% if (success) { %>
            <div class="alert alert-success">
                <i class="fas fa-check-circle"></i>
                <%= success %>
            </div>
        <% } %>
        
        <% if (error) { %>
            <div class="alert alert-error">
                <i class="fas fa-exclamation-triangle"></i>
                <%= error %>
            </div>
        <% } %>

        <!-- PT Data Section -->
        <% if (HN) { %>
            <div class="ptdata-section">
                <div class="section-header">
                    <h2 class="section-title">
                        <i class="fas fa-database"></i> สรุปผลการรักษา HN: <%= patient.HN %> <%= patient.fname %> <%= patient.lname %>
                    </h2>
                </div>

                <div class="section-actions">
                    <button class="btn-ptdata primary" onclick="showCreatePTDataForm()">
                        <i class="fas fa-plus"></i> เพิ่มข้อมูลใหม่
                    </button>
                </div>

                <!-- Empty State -->
                <div class="empty-state">
                    <div class="empty-state-icon">
                        <i class="fas fa-database"></i>
                    </div>
                    <h3 class="empty-state-title">ยังไม่มีข้อมูลสรุปผลการรักษา</h3>
                    <p class="empty-state-text">
                        ยังไม่มีข้อมูลสรุปผลการรักษา HN: <%= HN %><br>
                        คลิกปุ่ม "เพิ่มข้อมูลใหม่" เพื่อเริ่มต้น
                    </p>
                    <button class="btn-ptdata primary" onclick="showCreatePTDataForm()">
                        <i class="fas fa-plus"></i> เพิ่มข้อมูลใหม่
                    </button>
                </div>

            </div>

            <div id="createPTDataModal" class="modal">
                <div class="modal-content">
                    <div class="modal-header">
                        <h3><i class="fas fa-plus"></i> เพิ่มข้อมูลสรุปผลการรักษา</h3>
                        <button class="btn-ptdata tertiary" onclick="hideCreatePTDataForm()">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                    
                    <form class="modal-body" action="/ptData/<%= HN %>" method="POST">
                        <div class="form-group">
                            <label class="form-label">ประเภทการประเมิน *</label>
                            <select name="assessmentType" class="form-control" required>
                                <option value="">เลือกประเภทการประเมิน</option>
                                <option value="ROM">การเคลื่อนไหวของข้อต่อ</option>
                                <option value="MMT">ความแข็งแรงของกล้ามเนื้อ</option>
                                <option value="Pain">การประเมินความเจ็บปวด</option>
                                <option value="Function">การประเมินการทำงาน</option>
                                <option value="Balance">การประเมินการทรงตัว</option>
                                <option value="Gait">การประเมินการเดิน</option>
                                <option value="Other">อื่นๆ</option>
                            </select>
                        </div>

                        <div class="form-group">
                            <label class="form-label">ผลการประเมิน</label>
                            <textarea name="observations" class="form-control" rows="3" placeholder="ระบุ"></textarea>
                        </div>
                        
                        <div class="form-group">
                            <label class="form-label">วันที่ประเมิน *</label>
                            <input type="date" name="assessmentDate" class="form-control" required value="<%= new Date().toISOString().split('T')[0] %>">
                        </div>
                        
                        <div class="form-group">
                            <label class="form-label">ผู้ประเมิน</label>
                            <input type="text" name="assessor" class="form-control" placeholder="ชื่อผู้ประเมิน">
                        </div>
                        
                        <div class="form-actions">
                            <button type="button" class="btn-ptdata secondary" onclick="hideCreatePTDataForm()">
                                <i class="fas fa-times"></i> ยกเลิก
                            </button>
                            <button type="submit" class="btn-ptdata primary">
                                <i class="fas fa-save"></i> บันทึกข้อมูล
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Progress Chart Modal -->
            <div id="progressChartModal" class="modal">
                <div class="modal-content chart-modal">
                    <div class="modal-header">
                        <h3><i class="fas fa-chart-bar"></i> กราฟความก้าวหน้า</h3>
                        <button class="close-btn" onclick="hideProgressChart()">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                    
                    <div class="modal-body">
                        <div class="chart-container">
                            <canvas id="progressChart" width="400" height="200"></canvas>
                        </div>
                        
                        <div class="chart-legend">
                            <div class="legend-item">
                                <span class="legend-color" style="background: #667eea;"></span>
                                <span>ความแข็งแรง</span>
                            </div>
                            <div class="legend-item">
                                <span class="legend-color" style="background: #4ecdc4;"></span>
                                <span>การเคลื่อนไหว</span>
                            </div>
                            <div class="legend-item">
                                <span class="legend-color" style="background: #ff6b6b;"></span>
                                <span>ความเจ็บปวด</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="empty-state">
                <div class="empty-state-icon">
                    <i class="fas fa-search"></i>
                </div>
                <h3 class="empty-state-title">ค้นหาผู้ป่วยเพื่อบันทึกสรุปผลการรักษา</h3>
                <p class="empty-state-text">
                    กรอกข้อมูลในช่องค้นหาด้านบนเพื่อค้นหาผู้ป่วยและบันทึกสรุปผลการรักษา
                </p>
            </div>
        <% } %>
    </div>

    <!-- JavaScript -->
    <script src="/js/ptData.js"></script>
    <script src="/js/notifications.js"></script>
    
    <script>
        // Functions for modal management
        function showCreatePTDataForm() {
            document.getElementById('createPTDataModal').style.display = 'block';
        }

        function hideCreatePTDataForm() {
            document.getElementById('createPTDataModal').style.display = 'none';
        }

        function showProgressChart() {
            document.getElementById('progressChartModal').style.display = 'block';
        }

        function hideProgressChart() {
            document.getElementById('progressChartModal').style.display = 'none';
        }

        // Close modal when clicking outside
        window.onclick = function(event) {
            const createModal = document.getElementById('createPTDataModal');
            const chartModal = document.getElementById('progressChartModal');
            
            if (event.target === createModal) {
                hideCreatePTDataForm();
            }
            if (event.target === chartModal) {
                hideProgressChart();
            }
        }

        // Auto-hide alerts after 5 seconds
        setTimeout(function() {
            const alerts = document.querySelectorAll('.alert');
            alerts.forEach(function(alert) {
                alert.style.opacity = '0';
                setTimeout(function() {
                    alert.remove();
                }, 500);
            });
        }, 5000);
    </script>
</body>
</html>

<script>
// Helper functions for EJS
function getStatusIcon(status) {
    const icons = {
        'active': 'play',
        'completed': 'check',
        'pending': 'clock',
        'cancelled': 'times'
    };
    return icons[status] || 'question';
}

function getStatusName(status) {
    const names = {
        'active': 'ใช้งาน',
        'completed': 'เสร็จสิ้น',
        'pending': 'รอดำเนินการ',
        'cancelled': 'ยกเลิก'
    };
    return names[status] || 'ไม่ระบุ';
}

</script>

